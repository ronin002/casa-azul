export class Projeto {
    constructor(startDate, endDate, cliente, team, color) {
      this.startDate = startDate;
      this.endDate = endDate;
      this.cliente = cliente;
      this.team = team;
      this.color = color;
    }
  }